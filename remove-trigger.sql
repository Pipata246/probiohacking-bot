-- ============================================
-- УДАЛЕНИЕ ТРИГГЕРА ПОДПИСКИ (исправление рекурсии)
-- ============================================
-- Выполните этот код в Supabase SQL Editor
-- Это удалит триггер, который вызывал бесконечную рекурсию

-- Удаляем триггер, который вызывал бесконечную рекурсию
DROP TRIGGER IF EXISTS check_subscriptions_trigger ON public.users;

-- Удаляем функцию триггера
DROP FUNCTION IF EXISTS public.trigger_check_subscriptions();

-- Функция check_and_update_expired_subscriptions() остается
-- Она будет вызываться вручную через API при обновлении подписки
-- Это позволяет избежать рекурсии и контролировать когда проверять истекшие подписки
