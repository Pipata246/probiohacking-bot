# PROBIOHACKING Bot

Telegram бот с Mini App для персональной диагностики и рекомендаций по здоровью.

## 🚀 Быстрый старт

### 1. Установка

```bash
npm install
```

### 2. Настройка

Создайте файл `.env`:
```env
BOT_TOKEN=8313677702:AAFtaix5kgylEZ6sm9VF1nFkdR0ob9f3AMM
MINI_APP_URL=https://probiohacking-bot.vercel.app
WEBHOOK_URL=https://your-bot-server.com
PORT=3000
```

**Где:**
- `BOT_TOKEN` - токен вашего бота от @BotFather
- `MINI_APP_URL` - URL Mini App после деплоя на Vercel
- `WEBHOOK_URL` - URL сервера где работает бот (Railway, Render, VPS и т.д.)
- `PORT` - порт для Express сервера

### 3. Запуск бота

```bash
npm run bot
```

## 📦 Деплой на Vercel

### Через GitHub:

1. **Загрузите код на GitHub:**
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/username/repo.git
git push -u origin main
```

2. **Деплой на Vercel:**
   - Зайдите на https://vercel.com
   - Нажмите "Add New Project"
   - Выберите ваш репозиторий
   - Нажмите "Deploy"

3. **После деплоя:**
   - Скопируйте URL (например: `https://your-app.vercel.app`)
   - Обновите `.env`:
     ```env
     MINI_APP_URL=https://your-app.vercel.app
     ```
   - Перезапустите бота

### Через Vercel CLI:

```bash
npm i -g vercel
vercel
```

## 📁 Структура проекта

```
├── bot/
│   └── index.js          # Telegram бот
├── mini-app/             # Mini App
│   ├── index.html        # Главная страница
│   └── src/
│       ├── main.js       # JavaScript
│       └── style.css     # Стили
├── .env                  # Конфигурация (не в git)
├── .env.example          # Пример конфигурации
├── vercel.json           # Конфиг Vercel
└── package.json          # Зависимости
```

## ✅ Готово!

Бот работает и готов к деплою Mini App на Vercel.


## 📦 Деплой

### 1. Деплой Mini App на Vercel

**Через GitHub:**
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/username/repo.git
git push -u origin main
```

Затем:
1. Зайдите на https://vercel.com
2. Нажмите "Add New Project"
3. Выберите ваш репозиторий
4. Нажмите "Deploy"
5. Скопируйте URL (например: `https://your-app.vercel.app`)

### 2. Деплой бота на Railway/Render/VPS

**Railway (рекомендуется):**
1. Зайдите на https://railway.app
2. Создайте новый проект из GitHub
3. Добавьте переменные окружения:
   - `BOT_TOKEN` - ваш токен
   - `MINI_APP_URL` - URL с Vercel
   - `WEBHOOK_URL` - URL который даст Railway
   - `PORT` - 3000
4. Deploy!

**Render:**
1. Зайдите на https://render.com
2. Создайте новый Web Service
3. Подключите GitHub репозиторий
4. Добавьте переменные окружения
5. Deploy!

### 3. Обновите переменные

После деплоя обоих компонентов:
- В `.env` бота укажите реальный `MINI_APP_URL` с Vercel
- В `.env` бота укажите реальный `WEBHOOK_URL` с Railway/Render

## 🔧 Как работает

1. **Mini App** (Vercel) - статический сайт, открывается в Telegram
2. **Бот** (Railway/Render) - Express сервер с webhook
3. Telegram отправляет обновления на `WEBHOOK_URL/bot{TOKEN}`
4. Бот обрабатывает команды и отправляет ответы

## 📁 Структура проекта

```
├── bot/
│   └── index.js          # Telegram бот (Express + Webhook)
├── mini-app/             # Mini App для Vercel
│   ├── index.html        # Главная страница
│   └── src/
│       ├── main.js       # JavaScript
│       └── style.css     # Стили
├── .env                  # Конфигурация (не в git)
├── .env.example          # Пример конфигурации
├── vercel.json           # Конфиг Vercel (для Mini App)
└── package.json          # Зависимости (для бота)
```

## ✅ Готово!

Теперь у вас:
- ✅ Бот на webhook (продакшен-ready)
- ✅ Mini App готов к деплою на Vercel
- ✅ Правильная архитектура для масштабирования
